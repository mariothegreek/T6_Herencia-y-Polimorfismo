package Llamadas_herencia.model;

public interface LlamadaConCoste {
    //Interfaz que me permite hacer el cálculo de los costes totales en base a la clase en la que lo implemente.
    int getCosteTotal();
}
